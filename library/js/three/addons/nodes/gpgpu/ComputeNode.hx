package js.three.addons.nodes.gpgpu;

import js.three.addons.nodes.core.Node;
//import { NodeRepresentation, ShaderNodeObject } from "../shadernode/ShaderNode.js";

typedef ComputeNode = Dynamic;

/*export default class ComputeNode extends Node {
    isComputeNode: true;

    count: number;
    workgroupSize: number[];
    dispatchCount: number;

    constructor(computeNode: Node, count: number, workgroupSize?: number[]);
}

export const compute: (
    node: NodeRepresentation,
    count: number,
    workgroupSize: number[],
) => ShaderNodeObject<ComputeNode>;

declare module "../shadernode/ShaderNode.js" {
    interface NodeElements {
        compute: typeof compute;
    }
}*/
